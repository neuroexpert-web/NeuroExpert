# ===================================
# EXAMPLE ENVIRONMENT VARIABLES
# Copy this file to .env.local and fill in the values
# ===================================

# API Keys (REQUIRED)
# Use only these exact names for security
GOOGLE_GEMINI_API_KEY=your-gemini-api-key-here
JWT_SECRET=generate-random-32-char-string-here

# Optional Services
ANTHROPIC_API_KEY=your-claude-api-key-here
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# Database (if using PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/neuroexpert
POSTGRES_PASSWORD=your-secure-password
REDIS_PASSWORD=your-redis-password

# Monitoring (optional)
SENTRY_DSN=your-sentry-dsn
GRAFANA_ADMIN_PASSWORD=your-grafana-password

# IMPORTANT SECURITY NOTES:
# 1. NEVER use NEXT_PUBLIC_ prefix for secret keys
# 2. NEVER commit .env files to git
# 3. Use strong, unique passwords
# 4. Rotate keys regularly
# 5. Set different keys for development and production